version: "2"

run:
    timeout: 5m

linters:
    default: none # eski disable-all'ın karşılığı
    enable:
        - errcheck
        - govet
        - ineffassign
        - staticcheck
        - unused
        - misspell
        - bodyclose
        - nilerr
    settings:
        errcheck:
            check-blank: false
        govet:
            enable-all: true
            disable:
                - shadow
                - fieldalignment
        misspell:
            locale: US
    exclusions:
        rules:
            - path: "_test\\.go"
              linters:
                  - errcheck
                  - bodyclose
            - linters:
                  - errcheck
              text: "Error return value of `.*Close` is not checked"

formatters:
    enable:
        - gofmt # gofmt artık formatter section'a taşındı
